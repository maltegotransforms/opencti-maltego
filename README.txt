Set of Maltego transforms to explore OpenCTI data.

Use Maltego entities from the STIX 2 set : #ref

Local OpenCTI transforms leveraging the Maltego TRX library
	- Update the configuration file : config.py
	- Run build.sh
	- Copy the content of src/ to the execution directory specified in config.py
	- Import transforms.mtz
	- Install Python3 and its dependencies from requirements.txt

TODO
	- add a virtual env capability